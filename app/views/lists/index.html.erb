<h4> <%= link_to "Add a new list", new_list_path %> </h4>
<div class="container">
  <h1>Your Lists:</h1>
  <% @lists.each do |list| %>
    <%= link_to list.name, list_path(list) %>
  <% end %>
</div>
